The create_graph executable is used to create the graph.txt file which contains the adjacency matrix.
The cluster executable clusters this into the required clusters. The required command line arguments are number of buses,capacity of bus and
number of iterations for the algorithm.
The output of this cluster executable is CLUSTERS.txt in which each line holds a cluster of vertices.
Use C++17 to compile.